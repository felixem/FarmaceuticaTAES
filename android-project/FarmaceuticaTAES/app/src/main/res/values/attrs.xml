<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />

    </declare-styleable>

    <!-- Borde de EditText crear base de datos Background general-->
    <dimen name="radius1">10dip</dimen>

    <!-- Borde de EditText question de crear base de datos  -->
    <dimen name="radius2">10dip</dimen>

    <!-- Borde de EditText y TextView de repuestas -->
    <dimen name="radius3">10dip</dimen>

    <!-- Radio de borde de los items de la lista -->
    <dimen name="radius_lista">10dip</dimen>

    <!-- Tamaño Letra items lista -->
    <dimen name="text_size_lista">20sp</dimen>

    <dimen name="margin_topbot_list_item">4dp</dimen>
</resources>
